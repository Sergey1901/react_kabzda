00
let b: Array <string | number> типизация массива с твумя типами данных
примитивные типы пишуться автоматически

01 - компоненты (что от чего зависит)
function declaraton, expression, стрелочная ф-я
функцию нужно вызывать, без этого она просто определенна
Помошники (debugger, console.log, alert()) проверить куда их можно ставить.

02 - компонента ч-2 (рендеринг как компонента вызывает в себе другую компоненту)
Компонента - ф-я возвр. jsx
позволяет дробить код
<></> пустой тег
Отрисовка контента в реакте
Последовательноcть рендеринга(отрисовки), в какой последовательности отрисовываеться

03 - props
Иногда дробим компоненту что бы была структура.

эхпорт без default - экспортируеться в {т.н} {Raiting as R} переименование
отресовка это что то типа тега компоненты
передача пропсов, использование пропсов
передача через теги и ретюрны

